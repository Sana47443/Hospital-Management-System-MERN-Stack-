import {useEffect,useState} from 'react';import {api} from '../api/client';import Spinner from '../components/Spinner';
const fmt=d=>new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(d));
export default function DashboardPage(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{api.get('/dashboard').then(r=>setData(r.data)).catch(e=>setError(e.response?.data?.message||'Unable to load dashboard'))},[]);if(!data&&!error)return <Spinner/>;return <section><div className="page-head"><div><p className="eyebrow">Operations overview</p><h1>Dashboard</h1><p>Monitor today’s care activity and upcoming appointments.</p></div></div>{error&&<div className="alert error">{error}</div>}{data&&<><div className="metric-grid"><div className="metric"><span>Active patients</span><strong>{data.metrics.patients}</strong></div><div className="metric"><span>Active doctors</span><strong>{data.metrics.doctors}</strong></div><div className="metric"><span>Appointments today</span><strong>{data.metrics.today}</strong></div><div className="metric"><span>Completed total</span><strong>{data.statusCounts.completed||0}</strong></div></div><div className="card"><div className="card-head"><h2>Upcoming appointments</h2></div><div className="table-wrap"><table><thead><tr><th>Time</th><th>Patient</th><th>Doctor</th><th>Reason</th><th>Status</th></tr></thead><tbody>{data.upcoming.length?data.upcoming.map(a=><tr key={a._id}><td>{fmt(a.startTime)}</td><td>{a.patient?.firstName} {a.patient?.lastName}</td><td>Dr. {a.doctor?.firstName} {a.doctor?.lastName}<small>{a.doctor?.specialization}</small></td><td>{a.reason}</td><td><span className={`badge ${a.status}`}>{a.status}</span></td></tr>):<tr><td colSpan="5" className="empty">No upcoming appointments.</td></tr>}</tbody></table></div></div></>}</section>}
