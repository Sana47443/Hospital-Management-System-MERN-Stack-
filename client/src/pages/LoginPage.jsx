import {useState} from 'react';import {Navigate,useLocation,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
export default function LoginPage(){const {user,login}=useAuth();const nav=useNavigate();const loc=useLocation();const [form,setForm]=useState({email:'admin@hospital.local',password:'Admin123!'});const [error,setError]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e){e.preventDefault();setBusy(true);setError('');try{await login(form.email,form.password);nav(loc.state?.from||'/');}catch(e){setError(e.response?.data?.message||'Unable to sign in');}finally{setBusy(false)}}return <div className="login-page"><div className="login-panel"><div className="brand login-brand"><div className="brand-mark">N</div><div><strong>Northstar Hospital</strong><small>Management Console</small></div></div><h1>Welcome back</h1><p>Sign in to coordinate patient care and hospital operations.</p><form onSubmit={submit}><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required minLength="8"/></label>{error&&<div className="alert error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><div className="demo-note">Demo admin credentials are prefilled after running the seed script.</div></div><div className="login-art"><div><span>Care, coordinated.</span><h2>A modern command center for patient, doctor, and appointment operations.</h2></div></div></div>}
